# notes configuration. Copy this file to .env and edit it if you want to change anything.
#
# The app runs with zero config. This file only exists so the port is easy to change.
# .env is gitignored, so nothing here is committed.

# Port for the API (and the single-port production server).
PORT=5190
# The Vite dev client runs on 5189 (see client/vite.config.ts).
